<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="{{ asset('css/bucketlist.css') }}">
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {# {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
        
        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %} #}
    </head>
    <body>
        <nav>
            <ul>
                <li><a href="{{ path('app_main') }}">Acceuil</a></li>
                <li><a href="{{ path('home') }}">Bucket List</a></li>
                <li><a href="{{ path('abousUs') }}">Mentions légales</a></li>
                <li><a href="{{ path('list') }}">List</a></li>
            </ul>
        </nav>
            {% block body %}{% endblock %}
        </div>
        <footer>
            @Bucket-List {{ "now"|date("Y") }}
        </footer>
    </body>
</html>
